# Sample Output

This directory contains example output files generated by the corresponding C++ simulation.

These files are provided solely to allow users to execute the accompanying Jupyter notebooks without first running the simulations.

Users who wish to generate new datasets may simply replace these files with outputs produced by the C++ code.
