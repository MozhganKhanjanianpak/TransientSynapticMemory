#ifndef PARAMETERS_H
#define PARAMETERS_H

inline int H(int z)
{
    return (z > 0) ? 1 : 0;
}

inline int H(double z)
{
    return (z > 0.0) ? 1 : 0;
}

/////////////////////////////////////////////////////////////
//
// Model parameters
//
// This file contains all parameters used throughout the
// transient synaptic memory model.
//
// Changing values here automatically updates the whole
// simulation.
//
/////////////////////////////////////////////////////////////

/////////////////////////////////////////////////////////////
// Realization number
/////////////////////////////////////////////////////////////

constexpr int ens = 2;

/////////////////////////////////////////////////////////////
// Network
/////////////////////////////////////////////////////////////
constexpr int N = 2000;

constexpr double p = 0.1;

constexpr double E = 0.8;              // Excitatory fraction

/////////////////////////////////////////////////////////////
// Synapses
/////////////////////////////////////////////////////////////
constexpr double W0E = 1.0;
constexpr double W0I = -4.0;

constexpr int TE = 5;  // Excitatory synaptic maximum lifetime
constexpr int TI = 7;  // Inhibitory synaptic maximum lifetime

/////////////////////////////////////////////////////////////
// Neurons
/////////////////////////////////////////////////////////////
constexpr int D = 5;

/////////////////////////////////////////////////////////////
// Simulation
/////////////////////////////////////////////////////////////
constexpr int tmax = 150;   // Simulation duration (time steps)

constexpr double eta = 0.02; // Initial activation probability

#endif
